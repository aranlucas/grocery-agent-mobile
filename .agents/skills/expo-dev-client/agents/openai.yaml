interface:
  display_name: "Expo Dev Client"
  short_description: "Create and use Expo development clients when Expo Go is not enough"
  default_prompt: "Use $expo-dev-client to decide whether an Expo development client is required, build it locally with CNG and the platform toolchain, and connect it to Metro."
